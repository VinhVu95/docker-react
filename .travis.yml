sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t vinhvu/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true vinhvu/docker-react npm run test

